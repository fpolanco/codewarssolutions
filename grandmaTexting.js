function textin(str){
    // identify the three words to turn to 2
      //find the method to change
      //look through string google
      //if we find them them change to 2
      //return new string with replaced 2
      console.log(str)
      result = str.replace(/too|two|to/gi, 2)
        return result
      
    }